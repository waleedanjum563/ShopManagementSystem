@model Sales_And_Inventory_MIS.Models.Expense
@using Sales_And_Inventory_MIS.Models
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm("Search", "Expenses"))
{

    <b>From</b>
    <input class="form-control text-box single-line" data-val="true" data-val-date="The field Date must be a date." data-val-required="The Date field is required." id="Date" name="DateFrom" type="date" value="">
    <b>To</b>
    <input class="form-control text-box single-line" data-val="true" data-val-date="The field Date must be a date." data-val-required="The Date field is required." id="Date" name="DateTo" type="date" value="">

    <input type="hidden" value="Filter" name="searchtype">

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </div>

}

@using (Html.BeginForm("Search", "Expenses"))
{

    <input name="text" placeholder="text" type="text" />
    <input type="hidden" value="search" name="searchtype">

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </div>

}

@using (Html.BeginForm("Search", "Expenses"))
{ 
     @Html.AntiForgeryToken()
  
       DbContextClass db = new DbContextClass();

       
    <select name="type">
        @foreach(var type in db.expenseType)
        {
            <option>@type.name</option>
        }
    </select>

    <input type="hidden" value="type" name="searchtype">

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </div>

}
@using (Html.BeginForm("Search", "Expenses"))
{
    @Html.AntiForgeryToken()

    DbContextClass db = new DbContextClass();

    <select name="entity">
        @foreach (var type in db.entity)
        {
            <option value="@type.name">@type.name</option>
        }
    </select>

    <input type="hidden" value="entity" name="searchtype">

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </div>

}

@using (Html.BeginForm("Search", "Expenses"))
{
    <b>From</b>
    <input class="form-control text-box single-line" data-val="true" data-val-date="The field Date must be a date." data-val-required="The Date field is required." id="Date" name="DateFrom" type="date" value="">
    <b>To</b>
    <input class="form-control text-box single-line" data-val="true" data-val-date="The field Date must be a date." data-val-required="The Date field is required." id="Date" name="DateTo" type="date" value="">

    DbContextClass db = new DbContextClass();


    <select name="type">
        @foreach (var type in db.expenseType)
        {
            <option value="@type.name">@type.name</option>
        }
    </select>

    <input type="hidden" value="typedate" name="searchtype">

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </div>

}
@using (Html.BeginForm("Search", "Expenses"))
{
    <b>From</b>
    <input class="form-control text-box single-line" data-val="true" data-val-date="The field Date must be a date." data-val-required="The Date field is required." id="Date" name="DateFrom" type="date" value="">
    <b>To</b>
    <input class="form-control text-box single-line" data-val="true" data-val-date="The field Date must be a date." data-val-required="The Date field is required." id="Date" name="DateTo" type="date" value="">

    DbContextClass db = new DbContextClass();

    <select name="entity">
        @foreach (var type in db.entity)
        {
            <option>@type.name</option>
        }
    </select>

    <input type="hidden" value="entitydate" name="searchtype">

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </div>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
